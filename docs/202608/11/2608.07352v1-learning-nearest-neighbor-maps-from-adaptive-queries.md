---
title: Learning Nearest-Neighbor Maps from Adaptive Queries
title_zh: 从自适应查询中学习最近邻映射
authors: "Hadley Black, Geelon So"
date: 2026-08-07
pdf: "https://arxiv.org/pdf/2608.07352v1"
tags: ["query:active-clust"]
score: 6.0
evidence: 通过自适应查询恢复隐藏点集，与主动聚类的查询策略相关
tldr: "本文研究用自适应最近邻查询重构隐藏点集的问题。作者将之前局限于超立方体与单位球面的分析推广到一般范数，证明最坏情况查询复杂度为Θ(nκ)，κ为亲吻数。针对欧氏球面，利用随机化维度减少预处理，将查询复杂度从O(nd)降至O(min(n,d))；而球体域即使n=2也需exp(Ω(d))次查询。该结果揭示了球面与球体在查询复杂度上的显著分歧，并给出紧界。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.07352v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 755, \"height\": 694, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.07352v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 453, \"height\": 866, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.07352v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 566, \"height\": 477, \"label\": \"Table\"}]"
motivation: 此前仅研究布尔超立方体与单位球面，本文旨在一般范数域上确定最近邻查询重构隐藏点集的精确边界，并探索欧氏球面与球体的复杂度差异。
method: "利用亲吻数推导通用查询复杂度下界；设计随机化维度降低预处理将球面查询复杂度从O(nd)降至O(min(n,d))；构造球体及锥上的指数下界。"
result: "最坏情况查询复杂度为Θ(nκ)；球面达O(min(n,d))，球体需exp(Ω(d))（n=2亦然），锥需n exp(Ω(d))。"
conclusion: 球面与球体在查询复杂度上存在指数鸿沟，维度降低在球面有效，球体本质困难，为自适应点集重构提供紧界。
---

## 摘要
我们研究了从自适应查询中学习最近邻映射的问题，这等价于以下通过最近邻查询预言机重构隐藏集H的问题。令K是赋范空间(\mathbb{R}^d, \|\cdot\|)中的紧致域，H是K中n个点的隐藏集。查询q∈K时，预言机返回H中与q距离最小的某个h∈H。要精确恢复H需要多少次查询？先前的工作已在特定域（即布尔超立方体和ℓ2单位球面）中研究了这个问题。我们推广了先前的工作，并证明了紧的最坏情况查询复杂度界为Θ(nκ)，其中κ是底层范数的接吻数。在欧几里得范数中，获得κ的紧渐近界是一个重要的开放问题，尽管已知κ=exp(Θ(d))。我们的第二组结果表明，即使在自然的欧几里得域中，对d的指数依赖也是必需的：在球体中需要exp(Ω(d))次查询，即使当n=2时，在锥中需要n exp(Ω(d))次查询。最后，我们证明了欧几里得球面上一个更锐利的上界。这里，通过降维预处理步骤，d可以被替换为min(n,d)。这是Prabhu-Woodruff (ICML 2024)过程的一个随机化版本，我们将查询复杂度从O(nd)改进到O(min(n,d))。这揭示了球面与球体之间的显著对比：当n=O(1)时，球面拥有O(1)次查询算法，而球体需要exp(Ω(d))。

## Abstract
We study the problem of learning nearest-neighbor maps from adaptive queries, which is equivalent to the following problem of reconstructing a hidden set $H$ via a nearest-neighbor query oracle. Let $K \subset \mathbb{R}^d$ be a compact domain in a normed space $(\mathbb{R}^d,\| \cdot\|)$ and let $H \subset K$ be a hidden set of $n$ points. Upon querying $q \in K$, the oracle returns some $h \in H$ with minimum distance from $q$. How many queries are required to exactly recover $H$? Previous work has studied this question in specific domains, namely the Boolean hypercube and the $\ell_2$-unit sphere. We generalize previous work and prove the tight worst-case query complexity bound of $Θ(nκ)$, where $κ$ is the kissing number of the underlying norm.   In the Euclidean norm, obtaining tight asymptotic bounds on $κ$ is a significant open question, although it is known that $κ= \exp(Θ(d))$. Our second set of results shows that an exponential dependence on $d$ is required even in natural Euclidean domains: $\exp(Ω(d))$ queries are needed in the ball, even when $n=2$, and $n\exp(Ω(d))$ queries are needed in the cone.   Lastly, we prove a sharper upper bound in the Euclidean sphere. Here, $d$ can be replaced by $\min(n,d)$ via a dimension reduction preprocessing step. This is a randomized version of a procedure due to Prabhu-Woodruff (ICML 2024) where we improve the query complexity from $O(nd)$ to $O(\min(n,d))$. This reveals a striking contrast between the sphere and the ball: when $n = O(1)$, the sphere admits an $O(1)$ query algorithm, whereas the ball requires $\exp(Ω(d))$.