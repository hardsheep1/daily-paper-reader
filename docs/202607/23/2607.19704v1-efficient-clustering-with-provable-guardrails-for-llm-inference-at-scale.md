---
title: Efficient Clustering with Provable Guardrails for LLM Inference at Scale
title_zh: 具有可证明护栏的高效聚类以支持大规模LLM推理
authors: "Longshaokan Wang, Wai Tsang Keung, Punit Ghodasara, Roman Wang, Ali Dashti, Francesc Moreno-Noguer"
date: 2026-07-22
pdf: "https://arxiv.org/pdf/2607.19704v1"
tags: ["query:ac"]
score: 8.0
evidence: 使用聚类方法优化大语言模型推理
tldr: 大型语言模型推断面临成本与延迟瓶颈，现有聚类方法无法在百万级规模下同时保证每个样本与代表的相似度、精确匹配类别属性。提出两阶段算法：先用Mini-batch K-Means生成初始簇，再通过Johnson-Chvatal启发式方法贪心选择代表，严格保证相似度和属性约束。算法复杂度O(nd+n²d/K)，在3800万用户推荐系统中降低50倍成本与延迟，且保持个性化。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.19704v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1587, \"height\": 166, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.19704v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1498, \"height\": 1119, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.19704v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1573, \"height\": 462, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.19704v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1558, \"height\": 715, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.19704v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1248, \"height\": 886, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.19704v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1566, \"height\": 502, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.19704v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1412, \"height\": 732, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.19704v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1567, \"height\": 757, \"label\": \"Table\"}]"
motivation: 现有聚类方法无法在百万级规模下同时保证每个样本与代表的相似度及类别属性匹配，导致LLM推断成本高昂。
method: 两阶段算法：先用Mini-batch K-Means初始聚类，再用Johnson-Chvatal Set Cover启发式选取代表，严格满足相似度和属性约束。
result: 算法在3800万用户数据集上比常见方法快10-1000倍，降低50倍下游成本与延迟，成功上线生产。
conclusion: 所提方法能在大规模LLM推断中实现可验证的质量控制，显著降低计算成本，支持实际部署。
---

## 摘要
将基于LLM的应用扩展到数百万用户受到现代基础模型的推理成本和延迟的瓶颈。一个自然的解决方案是对输入进行聚类，并且仅在聚类代表上调用LLM，让其他成员继承输出——但这只有在每个成员可测量地接近其代表时才安全。现有的聚类方法在大规模场景下无法提供这种每样本质量控制：没有一种方法能够同时保证最小的簇内相似性、分类属性的精确匹配以及扩展到数千万样本的可扩展性。我们提出了一种两阶段算法，首先使用Mini-batch K-Means生成初始簇，然后在每个初始簇内贪婪地选择代表——这一步相当于在嵌入空间中用于α球集覆盖的Johnson-Chvatal启发式算法。该算法通过构造精确地强制执行相似性和属性护栏，并且对于n个样本、特征维度d和K个初始簇，运行时间为O(nd + n^2 d/K)，内存为O(nd + n^2/K^2)——当K与n成比例增长时，复杂度关于n是线性的。我们在内部和公共数据集上针对常见聚类方法提供了基准测试：我们的方法不仅提供了每样本护栏，而且运行速度提高了10-1000倍，并且可以扩展到大多数标准方法难以处理的数据规模。在基于人物的推荐系统上部署于3800万客户时，该聚类方法将下游成本和延迟降低了50倍，同时保持了个性化，并解锁了生产发布。

## Abstract
Scaling LLM-based applications to millions of users is bottlenecked by the inference cost and latency of modern foundation models. A natural fix is to cluster the inputs and call the LLM only on cluster representatives, letting other members inherit the output -- but this is only safe if each member is measurably close to its representative. Existing clustering methods do not offer such per-sample quality control at scale: none jointly guarantee a minimal within-cluster similarity, exact matching of categorical attributes, and scalability to tens of millions of samples. We propose a two-stage algorithm that generates initial clusters with Mini-batch K-Means, then greedily selects representatives within each initial cluster -- a step equivalent to the Johnson-Chvatal heuristic for Set Cover over alpha-balls in embedding space. The algorithm enforces the similarity and attribute guardrails exactly by construction, and runs in $O(nd + n^2 d/K)$ time and $O(nd + n^2/K^2)$ memory for $n$ samples, feature dimension $d$, and $K$ initial clusters -- linear in $n$ when $K$ grows proportionally with $n$. We provide benchmarks against common clustering methods on internal and public datasets: our method not only delivers per-sample guardrails but also runs 10-1000x faster and scales to data sizes where most standard methods become intractable. Deployed on 38 million customers for a persona-based recommender, the clustering method cut downstream cost and latency by 50-fold while preserving personalization and unblocked the production launch.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **问题**：大规模语言模型（LLM）在电商等场景的推理成本与延迟极高，无法直接服务数千万用户。
- **常见解法**：将输入聚类，仅在簇代表上调用LLM，其他成员继承输出，从而大幅降低计算量。
- **挑战**：现有聚类方法无法同时满足三项关键要求：
    - 每个样本与其代表之间的语义相似度 ≥ 用户指定阈值 α（质量护栏）；
    - 簇内样本的指定分类属性（如家庭组成）完全一致（安全护栏）；
    - 可线性或近线性扩展到千万级样本规模（n ~ 10⁷）。
- **论文目标**：提出一种两阶段聚类算法，同时满足上述三个要求，并在实际生产系统中验证其效果。

## 2. 方法论：核心思想、关键技术细节

- **总体思路**：先将输入嵌入到向量空间，通过两阶段聚类：  
  - **第一阶段**：使用 Mini-batch K‑Means 生成 K 个初始簇（复杂度低、可扩展）。  
  - **第二阶段**：在每个初始簇内，将代表选择问题转化为集合覆盖（Set Cover）问题，并使用 Johnson‑Chvátal 贪心启发式算法求解。
- **具体流程（Algorithm 1）**：
    1. 对所有样本计算嵌入向量（使用 Sentence‑BERT 等模型）。
    2. 在嵌入上运行 Mini‑Batch K‑Means，得到 K 个初始簇 {C_k}。
    3. 对每个初始簇 C_k：
        - 计算簇内所有点对的余弦相似度 S_ij 和匹配矩阵 M_ij（M_ij=1 当且仅当 S_ij ≥ α 且属性相等）。
        - 初始化未覆盖集合 U = C_k，代表集合 R = ∅。
        - 贪心循环：每次选取能覆盖最多未覆盖点的点作为代表，将其及其覆盖点从 U 中移除，并分配簇标签。重复直到 U 为空。
- **关键性质**：
    - **严格护栏**：由构造保证每个样本与代表相似度 ≥ α 且属性相等。
    - **Set‑Cover 近似比**：在每个初始簇内，贪心选择的代表数量 ≤ OPT_k · (1 + ln |C_k|)。
    - **可选的再分配步骤（Algorithm 2）**：将每个样本重新分配到其最相似的合法代表，进一步提高平均相似度，同时保持护栏。
- **复杂度**（K 个初始簇大致平衡）：
    - 时间：O(nd + n²d/K)
    - 内存：O(nd + n²/K²)  
    - 当 K = Θ(√n) 时，第二阶段时间与空间复杂度为 O(nd)，即关于 n 线性。

## 3. 实验设计

- **数据集**：
    - **内部购物人物（Shopping Personas）**：100K 条客户画像。
    - **AG News**：100K 条新闻标题（公开）。
    - **Cosmopedia**：100K 条合成文本（公开）。
    - 选择 100K 规模是因为部分基线方法（Agglomerative、Spectral）在此规模以上已不可行。
- **基准方法**：
    - Mini‑batch K‑Means、K‑Means、Agglomerative Clustering (Ward)、BIRCH、Spectral Clustering、Gaussian Mixture
    - 对比时，先运行所提出方法（α 选为约 50 倍数据缩减），再固定基线方法的簇数与之相同。
- **评估指标**：
    - 平均簇内相似度、最小簇内相似度、低于 α 的样本比例、运行时间。
    - 额外分析了簇大小分布偏斜程度、尾修剪后的覆盖率。
    - 对 7,000 个代表-成员对，通过 LLM 评委对推荐结果评分（1-5），计算与相似度的 Pearson/Spearman 相关性。

## 4. 资源与算力

- **实验环境**：单台 AWS r7i.12xlarge 实例，**未使用并行化**。
- **算力细节**：未提供 GPU 型号、数量或详细训练时长。仅在 100K 样本上测试运行时间（秒级）。
- **生产部署**：38M 客户聚类运行时间未明确给出，但下游 LLM 部分节省 50 倍成本（从 $1,018,500 降至 $20,370）和耗时（从 485 天降至 9.7 天），说明聚类本身开销远小于 LLM。

## 5. 实验数量与充分性

- **实验数量**：三个不同领域的数据集（内部、新闻、通用文本），对比六种标准聚类方法，并进行了：
    - 相似度与推荐相关性验证（7 个相似度桶，15 个推荐/桶）。
    - 生产 A/B 测试（38M 客户）。
    - 超参数敏感性分析（K 值、batch size 等）。
    - 尾修剪效果分析。
- **充分性与公平性**：
    - 基准方法使用 scikit‑learn 默认超参数，簇数匹配，比较合理。
    - 公平点：由于基线本身不支持 α 约束，直接比较固定簇数下的相似度违规率是合理的。
    - 不足：只在 100K 样本上做精确对比，更大规模下基线方法因内存/时间不可行而无法直接比较；未与 DBSCAN、HDBSCAN 等大规模聚类方法对比。

## 6. 主要结论与发现

- **所提出方法**：
    - **严格满足护栏**：所有样本的簇内相似度 ≥ α，属性完全一致；基线方法有 3%~21% 的样本违规。
    - **速度快 10-1000 倍**：即使在相同簇数下，因为 O(n²) 计算被限制在初始簇内。
    - **簇大小高度偏斜**：前 4% 的簇覆盖 90% 的客户，启用了高效的尾修剪（额外 25 倍缩减）。
- **生产部署结果**：
    - 在 38M 客户的推荐系统中，α=0.77，精确匹配 5 个家庭属性。
    - LLM 查询生成步骤：成本从 $114,000 降至 $2,100，耗时从 22.8 天降至 0.4 天。
    - LLM 审核步骤：成本从 $1,018,500 降至 $20,370，耗时从 485 天降至 9.7 天。
    - 推荐质量损失极小：成员产品相关率仅比代表低 0.7%。
    - A/B 测试显示业务指标显著正向，已生产上线。
- **相似度作为代理**：成员-代表相似度与推荐相关性的 Pearson/Spearman 相关系数达 0.781/0.797（p<0.001），验证了相似度护栏的有效性。

## 7. 优点

1. **严格可证明的质量护栏**：通过构造保证，而非近似。
2. **可扩展性**：复杂度关于 n 线性（当 K=Θ(√n)），在实践中可处理数千万样本。
3. **兼具速度与效果**：比标准方法快两个量级，且能取得更好的簇内最差相似度。
4. **簇大小偏斜的实用优势**：可利用尾簇修剪进一步降低下游计算，而基线方法因簇均匀而无法做到。
5. **可选再分配**：在不增加簇数前提下提高平均相似度，灵活权衡。
6. **生产验证**：在大规模真实系统中证明有效，并产生显著业务影响。

## 8. 不足与局限

- **实验覆盖局限**：
    - 精确对比仅限 100K 样本，更大规模下基线的性能为估计缺失（如内存爆炸）。
    - 未与专门的大规模聚类方法（如 DBSCAN、HDBSCAN、FAISS 等方法）比较。
    - 仅使用了余弦相似度，未验证其他距离度量（如欧氏距离、内积等）。
- **超参数依赖**：需要手动选择 α（质量阈值）和 K（初始簇数），虽提供了敏感性分析但无自动选取机制。
- **初始聚类可造成分裂损失**：若 Mini‑Batch K‑Means 将相似的样本分入不同初始簇，它们无法共享代表，可能轻微增加最终簇数（实验中约 30-50% 增长）。
- **再分配步骤的副作用**：虽然提高了平均相似度，但使簇大小分布更平坦，降低了尾修剪效率。
- **仅适用于对称相似度函数**：论文假设相似度对称（如余弦），对于非对称度量需修改算法。
- **缺乏更复杂相似度模型**：当前以嵌入余弦相似度近似 LLM 输出一致性，可能存在更精准的代理（如预测 LLM 结果相似度的学习函数），论文列为未来方向。

（完）
