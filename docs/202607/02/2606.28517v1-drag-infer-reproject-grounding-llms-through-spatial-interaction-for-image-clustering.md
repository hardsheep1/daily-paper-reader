---
title: "Drag, Infer, Reproject: Grounding LLMs through Spatial Interaction for Image Clustering"
title_zh: 拖动、推断、重投影：通过空间交互将大语言模型应用于图像聚类
authors: "Yang Liu, Xuxin Tang, Jiahao Xu, Chris North"
date: 2026-06-26
pdf: "https://arxiv.org/pdf/2606.28517v1"
tags: ["query:ac"]
score: 9.0
evidence: 直接利用大语言模型进行图像聚类，并通过用户拖拽交互实现类似主动学习的反馈机制
tldr: 图像聚类中用户的聚类准则常逐步形成，现有语义交互方法依赖预定义准则或嵌入。CriterionSI利用大语言模型从用户拖拽交互中推断并细化聚类准则，结合局部拖拽指导全局重投影。仿真评估表明，该方法能逐步发现目标准则并产生准则对齐的聚类布局。贡献在于实现了基于自然语言理解的交互式聚类，无需预设准则。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.28517v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1658, \"height\": 643, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28517v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1554, \"height\": 833, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28517v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 879, \"height\": 494, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28517v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 860, \"height\": 504, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.28517v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 759, \"height\": 261, \"label\": \"Table\"}]"
motivation: 用户聚类准则通常逐步形成，现有方法依赖固定准则或嵌入，无法灵活适应用户意图的变化。
method: CriterionSI通过LLM从用户顺序拖拽中推断准则，结合局部操作指导全局重投影。
result: 仿真实验证明方法能从序列交互中发现并细化目标准则，生成对齐准则的聚类布局。
conclusion: CriterionSI利用LLM实现交互式准则推断，提升了图像聚类中用户意图理解的灵活性和准确性。
---

## 摘要
降维和语义交互通过使相似性结构可见和可操作来支持图像聚类。现有的语义交互方法通过直接操作编码用户的聚类准则（用户可解释的语义维度，例如动作、位置或情绪）来引导重投影，从而让用户直接控制最终的布局。然而，这些方法通常依赖于学习到的嵌入或预定义的准则。在实践中，用户的聚类准则往往逐渐显现，并通过交互得以细化，而不是一开始就完全清晰。在这项工作中，我们提出了CriterionSI（准则引导的语义交互），一种将增量拖动交互转化为准则引导的重投影的方法。CriterionSI使用大语言模型从用户的连续拖动中推断并细化聚类准则，同时将语义解释植根于人类提供的反馈，而不是固定的先验假设。CriterionSI将推断出的准则与局部拖动相结合，以指导全局重投影。基于模拟的评估和使用场景表明，CriterionSI能够从连续交互中发现并细化目标准则，并逐步生成符合准则的聚类布局。我们的代码和数据可在以下网址获取：https://github.com/4C79/CriterionSI。

## Abstract
Dimension reduction and semantic interaction support image clustering by making similarity structure visible and manipulable. Existing semantic interaction methods encode users' clustering criterion (a user-interpretable semantic dimension, e.g., action, location, or mood) from direct manipulation to steer reprojection, giving users direct control over the resulting layout. Yet they typically depend on learned embeddings or a predefined criterion. In practice, users' clustering criterion often emerges gradually and becomes refined through interaction rather than being fully clear at the outset. In this work, we present CriterionSI (Criterion-guided Semantic Interaction), a method that translates incremental drag interactions into criterion-guided reprojection. CriterionSI uses large language models to infer and refine the clustering criterion from sequential user drags, while grounding semantic interpretation in human-provided feedback rather than fixed prior assumptions. CriterionSI combines the inferred criterion with local drags to guide global reprojection. The simulation-based evaluation and usage scenario demonstrate that CriterionSI can discover and refine the target criterion from sequential interactions and progressively produce criterion-aligned clustering layouts. Our code and data are available at: https://github.com/4C79/CriterionSI.

---

## 论文详细总结（自动生成）

基于提供的论文内容，以下是对该论文的结构化、深入且客观的中文总结。

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：在图像聚类的可视化分析中，用户通常不会在一开始就有一个完全明确的聚类准则（如按“动作”或“情绪”聚类）。这个准则是在与可视化界面交互的过程中逐步形成和细化的。现有的语义交互方法要么依赖于预定义的准则（如用户需提供文本描述），要么将准则隐式地学习在模型权重中，无法处理用户意图的模糊性和渐进性。因此，如何从用户简单、模糊的交互（如拖拽图片）中理解并显式化其逐步演变的聚类意图，是一个核心挑战。
- **研究动机**：大语言模型（LLM）和视觉语言模型（VLM）的进步为理解复杂的语义提供了可能。作者旨在利用LLM的推理能力，将用户看似简单的“拖拽”操作，推断为具有明确语义标签的聚类准则，从而赋予系统“理解”用户的深层意图的能力。
- **整体含义**：这项工作提出了一种人机协同的聚类新范式。系统不再是等待用户下达明确的指令，而是通过分析用户的交互行为，主动推断用户的潜在意图，并以此为基础主动重排数据布局。这标志着可视化分析系统从“被动执行”到“主动推理”的转变。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：CriterionSI的核心思想是一个闭环流程：**拖拽 (Drag) -> 推断 (Infer) -> 重投影 (Reproject)**。系统将用户的每一次拖拽视为一个“证据”，通过LLM驱动的“准则追踪器”逐步累积和分析这些证据，最终“承诺”一个明确的聚类准则。一旦准则稳定，系统便利用LLM对所有图像进行基于该准则的分类，并结合用户局部的拖拽约束，进行全局的重投影更新。
- **关键技术细节**：
    1.  **交互捕获**：每次拖拽事件被形式化为一个元组，包含拖拽的锚点图像、拖拽前后邻域内的图像、以及位置信息。系统根据拖拽轨迹与邻域关系，将拖拽模式分为 `toward`, `away`, `both`, `unclear`。
    2.  **LLM驱动的准则追踪器**：
        - **准则推断**：每次拖拽后，LLM会根据锚点图像和邻域图像，推理出最可能解释这次拖拽行为的聚类准则，并给出一个置信度分数。为了减少LLM生成结果的随意性，系统提供了一个预定义的候选准词表（例如，来自数据集的元数据）。
        - **置信度门控**：当LLM给出的候选准则之间区分度不高时，系统会进行二次验证，判断当前邻域证据是否可靠，并决定是“保持”、“修改”还是“推迟”当前的准则假设。
        - **多步累积**：系统维护一个状态机，通过滑动窗口机制跟踪每个候选准则的累积支持度。只有当某个准则的累积分数连续超过阈值时，它才会被“激活”为活跃准则。如果出现新的冲突证据，只有在新的准则连续超过现有准则一定幅度时，才会发生切换。这避免了由于单次交互噪声导致的准则不稳定。
    3.  **准则引导的重投影**：
        - **准则约束**：一旦活跃准则被激活，系统利用LLM对所有图像进行基于该准则的值分配（例如，所有图片被分为“Joyful”、“Focused”、“Relaxed”）。这个值分配被用来计算一个“语义距离”矩阵：具有相同准则值的图像距离被拉近，不同值的被推远。
        - **拖拽约束**：对于涉及拖拽锚点的图像对，使用一个混合距离，融合了拖拽带来的新位置信息和上述语义距离。
        - **增量MDS**：最终，系统使用增量式多维缩放（MDS）来优化所有图像的2D位置，以同时满足视觉相似性、语义准则约束和拖拽约束。最近的几次拖拽锚点会被钉住，以防止早期交互的过度影响。

### 3. 实验设计：数据集、场景、基准与对比方法

- **数据集**：使用了 **Action40** 数据集的一个 **72 张图片的子集**。
- **场景/任务**：将图片按照 **Mood（情绪）** 分为三类：`Joyful`, `Focused`, `Relaxed`。这是一个极具挑战性的任务，因为情绪与视觉外观的关联性很弱。
- **基准 (Benchmark)**：作者将 **ISM（Interactive Semantic Mapping）** 作为对照组。ISM接收的是预定义的、正确的 `Mood` 准则，并直接使用该准则进行值分配和重投影，但**没有**拖拽交互。通过对比，可以单独评估CriterionSI中“拖拽驱动的准则追踪”模块的贡献。
- **对比方法**：
    - **WMDS**：基于多维缩放的交互方法，只使用拖拽信号。
    - **ImageSI**：基于深度学习的语义交互方法，使用拖拽和视觉信号。
    - **SpaceEditing**：潜空间编辑方法，使用拖拽和视觉信号。
    - **CriterionSI (Gemini cls)**：作者的完整方法，使用Gemini 2.5 Flash进行LLM推理和值分类，是**全自动管道**。
    - **CriterionSI (oracle cls)**：作者的消融版本，使用**真实的标签**进行值分类，以隔离准则追踪质量与LLM分类噪声的影响。

### 4. 资源与算力

- **未明确说明**：论文中提到了使用的模型（EVA-CLIP ViT-G/14 [17] 作为视觉编码器，LLM使用Gemini 2.5 Flash），但**没有明确给出训练或推理所需的具体GPU型号、数量或运行时长**。所有LLM调用都是通过API进行，这意味着算力消耗主要取决于调用的数量和API的响应时间，而非本地训练的算力。

### 5. 实验数量与充分性

- **实验数量**：
    - 主要定量评估基于一个**模拟实验**。作者设计了一个目标导向的拖拽模拟器，模拟用户进行交互。
    - 针对Mood任务，所有方法都在**3次独立运行**上取平均值和方差（图4）。
    - 提供了**一个详细的使用场景**（第4节），展示了系统在单次运行中的表现。
- **充分性与公平性**：
    - **局限性**：实验规模相对有限，仅在一个**72张图片**的数据集上进行了一个**单一任务**的评估。这很难充分证明方法的通用性和鲁棒性，尤其是在处理更大规模、更多样化的数据集时。
    - **客观性**：评估指标是 **Adjusted Silhouette Score**，这是一个常见的聚类质量评价指标。对比方法也足够有代表性。
    - **公平性**：所有方法都使用了相同的初始EVA-CLIP嵌入和MDS投影，确保了公平比较。消融实验（`oracle cls` vs. `Gemini cls`）的设计很合理，能有效分离不同模块的贡献。
    - **模拟器的偏差风险**：模拟器是目标导向的，产生的交互行为“完美”且一致，这排除了真实用户可能出现的错误、犹豫或意图中途变化的情况，可能会导致实验结果偏乐观。

### 6. 论文的主要结论与发现

1.  **有效性**：CriterionSI能够从用户的连续拖拽交互中成功推断出聚类准则，并逐步生成与准则高度对齐的聚类布局，在调整后轮廓系数指标上**显著优于** WMDS、ImageSI 和 SpaceEditing 等传统方法。
2.  **拖拽的价值**：通过对比CriterionSI (oracle cls) 与 ISM（无拖拽，但有先知准则），发现前者取得了更高的轮廓系数（0.84 vs 0.65）。这充分说明了**用户拖拽提供的局部排列信号**对于指导全局重投影布局具有重要价值，即使是已经知道了正确的聚类准则。
3.  **瓶颈**：CriterionSI全自动管道（Gemini cls）的表现（0.44）远低于使用真实标签的版本（oracle cls，0.84）。这表明**LLM对图像进行语义分类的精度是目前系统的性能瓶颈**，而非准则推断或重投影算法本身。
4.  **“正确”准则的重新定义**：实验发现，系统不一定总能收敛到用户心目中的“正确”标签（如“Action”可能被识别为“Body Posture”）。作者认为这并非失败，因为系统依然能提供一个有效的、语义一致的聚类布局，甚至可能揭示用户未明确考虑到的、但同样有意义的语义维度，体现了**人机协同构建意图**的理念。

### 7. 优点：方法或实验设计上的亮点

- **方法创新性**：将LLM作为“推理引擎”引入交互式可视化分析，实现了对用户模糊意图的实时、动态推断，是“具身LLM”在视觉分析领域的一个巧妙应用。其“证据累积”的机制非常鲁棒，能抵抗单次交互的模糊性。
- **意图显式化**：不同于以往将意图隐式编码在模型权重中，CriterionSI显式地输出一个具有语义名称的聚类准则（如“Mood”），这使得系统的“推测”过程对用户完全透明，便于用户理解、接受或拒绝。
- **人机协同**：方法深刻体现了人机共同构建意图的理念。系统提出的假设和用户的交互行为在状态机中“协商”，最终沉淀出一个人机双方共同认可的准则。
- **消融实验设计**：通过将LLM分类器替换为真实标签（`oracle cls`），非常清晰地分离并量化了“准则追踪”和“值分类”两个模块的贡献，精准定位了性能瓶颈。

### 8. 不足与局限

- **实验覆盖不足**：实验仅使用了一个小型数据集（72张图片）和单一场景（Mood）。方法的可扩展性和通用性在更大的数据集、更多样的任务（如按物体、地点、风格聚类）上尚未得到验证。
- **模拟器偏差**：主要评估基于模拟用户，其行为模式与真实用户（可能存在误差、探索性、意图反复）存在显著差异。真实用户实验是必要的，也是作者自己提出的未来工作。
- **LLM依赖与成本**：性能严重依赖于LLM（Gemini 2.5 Flash）的推理和分类能力。API调用可能带来延迟和成本问题，对于大规模交互式系统可能是一个瓶颈。
- **分类精度瓶颈**：实验明确指出了LLM分类精度是一个主要障碍。这意味着全自动管道的表现上限受限于模型的视觉-语言对齐能力，而这一点并非CriterionSI自身所能控制的。
- **交互成本**：系统需要足够的交互次数来累积证据并“激活”准则。对于用户不熟悉的、高度复杂的海量数据集，交互成本可能会很高。作者也提到了通过主动建议准则来降低成本的未来方向。

（完）
