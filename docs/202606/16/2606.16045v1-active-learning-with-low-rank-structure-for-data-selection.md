---
title: Active Learning with Low-Rank Structure for Data Selection
title_zh: 基于低秩结构的数据选择主动学习
authors: "Vincent Cohen-Addad, Sasidhar Kunapuli, Vahab Mirrokni, Mahdi Nikdan, David P. Woodruff, Samson Zhou"
date: 2026-06-14
pdf: "https://arxiv.org/pdf/2606.16045v1"
tags: ["query:active-clust"]
score: 8.0
evidence: 使用k中心和k均值聚类的主动学习数据选择方法
tldr: 针对数据选择问题，现有聚类方法假设几何结构，难以适应现代数据的全局代数结构。本文提出基于低秩近似和残差采样的新框架，通过行子集选择构造损失保持coreset。在嵌入满足Lipschitz条件下，可选出加权子集，使平均损失逼近全数据集，相对误差为1+ε，附加εΦ_k项。实验表明该方法优于均匀采样和聚类敏感度采样。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.16045v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 808, \"height\": 522, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16045v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 797, \"height\": 527, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16045v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1695, \"height\": 475, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16045v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1694, \"height\": 659, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16045v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1673, \"height\": 716, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16045v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1183, \"height\": 1582, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.16045v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1723, \"height\": 405, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.16045v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 774, \"height\": 412, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.16045v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 634, \"height\": 347, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.16045v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 592, \"height\": 264, \"label\": \"Table\"}]"
motivation: 现有数据选择方法依赖聚类假设，不适应现代数据的全局代数结构，需更通用的低秩近似框架。
method: 基于低秩近似和残差采样，通过行子集选择构造损失保持coreset，利用嵌入矩阵的代数结构。
result: 选择Õ(k+1/ε²)个加权点，平均损失达到(1+ε)相对误差，附加εΦ_k项，其中Φ_k为最优秩k近似代价。
conclusion: 提出低秩结构主动学习框架，理论保证与实证优势，拓展了数据选择方法的应用范围。
---

## 摘要
在数据选择问题中，目标是从数据中选择一个小的、有代表性的子集，用于高效地训练机器学习模型。Sener和Savarese [ICLR 2018] 表明，给定数据的嵌入表示和合适的几何假设，基于$k$-中心聚类的启发式方法可用于数据选择。Axiotis等人 [ICML 2024] 进一步探索了这一视角，提出了一种基于$k$-均值聚类和敏感性采样的数据选择方法。然而，这些方法依赖于数据集具有内在的几何结构，能够被聚类有效捕捉的假设，而许多现代数据集却拥有全局代数结构，更适合通过低秩近似或主成分分析来利用。在本文中，我们提出了一种新的数据选择框架，基于低秩近似和残差采样，通过行子集选择和保损核心集构建的视角来表述。给定满足温和正则条件的嵌入表示（可解释为Lipschitz连续性的代数或角度概念），我们证明可以选择一个加权子集，包含$\tilde{O}\left(k + \frac{1}{\varepsilon^2}\right)$个数据点，其平均损失在$(1+\varepsilon)$相对误差内逼近全数据集的平均损失，加上一个可加项$\varepsilon Φ_k$，其中$Φ_k$表示嵌入矩阵的最优秩-$k$近似代价。我们通过经验评估补充了这些理论保证，表明在一系列真实世界数据集上，我们的数据选择方法比基于均匀采样或基于聚类的敏感性采样的先前策略实现了更优的性能。

## Abstract
In the data selection problem, the objective is to choose a small, representative subset of data that can be used to efficiently train a machine learning model. Sener and Savarese [ICLR 2018] showed that, given an embedding representation of the data and suitable geometric assumptions, heuristics based on $k$-center clustering can be used to perform data selection. This perspective was further explored by Axiotis et. al. [ICML 2024], who proposed a data selection approach based on $k$-means clustering and sensitivity sampling. However, these methods rely on the assumption that the dataset exhibits intrinsic geometric structure that can be effectively captured by clustering, whereas many modern datasets instead possess global algebraic structure that is better exploited by low-rank approximation or principal component analysis.   In this paper, we introduce a new data selection framework based on low-rank approximation and residual-based sampling, formulated through the lens of row subset selection and loss-preserving coreset construction. Given an embedding representation of the data satisfying mild regularity conditions, which can be interpreted as algebraic or angular notions of Lipschitz continuity, we show that it is possible to select a weighted subset of $\tilde{O}\left(k + \frac{1}{\varepsilon^2}\right)$ data points whose average loss approximates the average loss over the full dataset within a $(1+\varepsilon)$ relative error, up to an additive $\varepsilon Φ_k$ term, where $Φ_k$ denotes the optimal rank-$k$ approximation cost of the embedding matrix. We complement these theoretical guarantees with empirical evaluations, demonstrating that on a range of real-world datasets, our data selection approach achieves improved performance over prior strategies based on uniform sampling or clustering-based sensitivity sampling.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究问题**：如何高效地从大规模数据集中选择一小部分最有信息量的子集，用于训练机器学习模型（特别是深度模型），从而在保持模型性能的前提下显著降低计算开销。
- **现有方法局限**：
  - 传统数据选择方法多基于聚类（如 $k$-center、$k$-means）。这些方法假设数据具有局部几何结构（如点之间的欧氏距离可以反映信息的多样性或覆盖性）。
  - 然而，现代数据集（特别是来自大语言模型微调任务的数据）往往展现出全局代数结构，而非局部聚类结构。聚类方法在高维数据中容易被噪声和分散的方向干扰，忽略主导方差方向（即“主成分”）。
  - 现有基于不确定性的主动学习方法在大规模批处理场景中面临计算瓶颈和冗余问题。
- **核心动机**：作者认为，低秩近似（Low-Rank Approximation）能够更好捕捉数据的全局谱方向（主导方差），因此基于低秩近似的采样应该比基于聚类的几何覆盖策略更有效、更理论可靠。
- **整体含义**：本文提出一种新的主动学习框架，从代数结构（而非几何距离）出发，将数据选择问题重新定义为“行子集选择”+“保损失核心集构建”，并提供严格的理论保证。

## 2. 论文提出的方法论

- **核心思想**：利用低秩近似（例如 SVD）提取数据的主成分子空间，再将每个数据点分解为：
  - **投影分量** $v(y)$：在低秩子空间 $V$ 上的投影，由前 $k$ 个基向量线性表示。
  - **残差分量** $r(y)$：与 $V$ 正交的分量。
- **核心假设（Assumption 2.1）**：损失函数 $\ell$ 满足两类正则性条件：
  1. **正交方向 Lipschitz 条件**：$|\ell(y) - \ell(v(y))| \le \lambda \|r(y)\|_2^2$，说明在子空间外，损失变化受残差范数的二次控制。
  2. **子空间内分解近似**：$|\ell(v(y)) - \sum_i \alpha_i^2 \ell(v_i)| \le \gamma \sum_i |\alpha_i^2 - 1| \ell(v_i)$，说明子空间内损失可被主方向损失加权近似。
- **关键技术**：使用 **灵敏度采样（Sensitivity Sampling）**，以每个样本在损失分解中的贡献率（结合投影损失与残差异常性）作为采样概率，选取少量点并赋予权重。
- **算法流程（以算法 2 为例）**：
  1. 对数据矩阵 $D$ 做秩-$k$ SVD，得到子空间 $V=\mathrm{span}(v_1,\dots,v_k)$。
  2. 对每个点 $x$，计算其投影系数 $\alpha_i$ 和残差 $r(x)$。
  3. 计算灵敏度得分 $\sigma(x) = (\gamma+1)(\alpha_1^2\ell(v_1)+\dots) + \gamma k \xi + \lambda\|r(x)\|_2^2$。
  4. 归一化得到采样概率 $p(x)=\sigma(x)/\sum \sigma(y)$。
  5. 以概率 $p(x)$ 采样 $s = \lceil \varepsilon^{-2}(2+2\varepsilon/3)\rceil$ 个点。
  6. 为每个采样点赋予权重 $w(x)=1/(s\cdot p(x))$。
- **核心定理（Theorem 2.2）**：该算法能以概率 $\ge 0.9$ 构造大小为 $O(1/\varepsilon^2)$ 的加权子集 $S$，使得：
  \[
  \left|\sum_{x\in D}\ell(x) - \sum_{x\in S} w(x)\ell(x)\right| \le \varepsilon\left(\sum_{x\in D}\ell(x) + \gamma\|D\|_F^2 + \gamma k n \max\ell + 2\lambda\Phi_k(D)\right)
  \]
  其中 $\Phi_k(D)$ 为最优秩-$k$ 近似误差。
- **另一个变体（Theorem 2.3）**：通过行子集选择（Row Subset Selection）确保选出的所有点都属于原始数据集 $D$，适用于需要标签的场景。

## 3. 实验设计

- **数据场景**：
  - **信用卡违约数据（UCI）**：30,000 样本，23 维特征，二分类（22% 正例）。
  - **LLM 微调任务（三个数据集）**：
    - **GSM8k**（数学推理）：7.47k 训练，1.32k 测试。
    - **ViGGO**（对话生成）：5.1k 训练，1.08k 测试。
    - **SQL 生成**：30k 训练，1k 测试。
- **模型 backbone**：
  - **Llama3-8B-Instruct**
  - **Qwen2.5-3B**（用于验证泛化性）
- **对比方法**：
  - **Full training**（全量数据训练）
  - **Uniform sampling**（随机均匀采样）
  - **K-Center**（Sener & Savarese, 2018）
  - **Graph Cut**（基于子模最大化）
  - **Clustering-based Sensitivity Sampling**（Axiotis et al., ICML 2024）
- **嵌入**：默认使用 BERT 嵌入；在消融研究中加入 GTR-base 嵌入验证嵌入可分离性。
- **评估指标**：验证集准确率；损失近似误差（$\Delta(S)$）；投影/聚类误差；梯度范数近似能力。

## 4. 资源与算力

- **文中未明确说明 GPU 型号、数量或总训练时长**。
- 仅提及：Llama3-8B 和 Qwen2.5-3B 使用 Adam 优化器，训练 1 个 epoch；学习率为 $6\times10^{-6}$（GSM8k）、$4\times10^{-5}$（ViGGO）、$3\times10^{-5}$（SQL）。
- **算力讨论**：文章提到，低秩方法与聚类方法均需对 $k=20\%$ 的数据做前向传播（计算嵌入），若反向传播成本按前向的 2 倍算，这相当于完整训练 1 epoch 的 $6.67\%$ 算力。

## 5. 实验数量与充分性

- **实验数量**：
  - 信用卡数据：5 个样本量（1000–5000）× 100 次重复 × 3 方法 = 1500 次试验。
  - LLM 微调：3 个数据集 × 3 个采样比例（25%/12.5%/6.25%）× 5 个基线 + 本文方法 + full = 6 种采样方法，共约 54 组实验（不含重复）。
  - 消融研究：
    - 不同 $\lambda$（0, 0.01, 0.1, 1.0）与不同 $k$（1%–51%）组合，绘制损失近似热力图。
    - 目标函数消融（loss vs. 梯度范数）。
    - 嵌入消融（BERT vs. GTR）。
    - 模型消融（Llama3-8B vs. Qwen2.5-3B）。
    - 转移性实验（用 Llama 选的子集训练 Qwen）。
    - 基础选择算法消融（均匀、k-Medians、杠杆得分）。
- **充分性与公平性评价**：
  - **优点**：覆盖多数据集、多模型、多嵌入、多目标，消融完整，重复 100 次取均值，结果具有统计代表性。
  - **潜在不足**：
    - 仅测试了 3B 和 8B 规模的 LLM，未测试更大模型（如 70B+ 或 200B+）。
    - 数据集类型有限（数学、对话、SQL），未覆盖图像、多模态等场景。
    - 对 $\lambda$、$\gamma$ 等超参数的选择依赖调优，文中未给出系统化的鲁棒性分析。

## 6. 论文的主要结论与发现

- **理论核心结论**：低秩灵敏度采样可以构造损失保持核心集，误差受控于最优秩-$k$ 近似代价，理论保证了在数据满足代数结构假设时的有效性。
- **实证核心结论**：
  - 低秩方法在 **所有任务** 上一致性优于均匀采样和聚类敏感性采样。
  - 在 ViGGO 上，使用 Llama3-8B 选择的子集可以直接转移给 Qwen2.5-3B 使用，且训练效果超越 Qwen 自身选子集的效果，说明所选子集捕捉到数据内在属性而非模型伪影。
  - 当数据本身是低秩结构（如 GSM8k）时，低秩方法的近似误差显著小于聚类方法（Fig. 2）。
- 低秩方法对 $\lambda$ 的选择比聚类方法更鲁棒（Fig. 4）。

## 7. 优点

- **理论保障强**：提供了严格的核心集误差界，直接关联数据代数结构（最优低秩近似误差）。
- **缓解聚类方法缺陷**：在高维数据中，聚类方法容易忽略主方向，低秩方法直接瞄准主导方差方向，更本质。
- **实验覆盖全面**：不仅做端到端准确率对比，还做了损失近似精度、梯度近似能力、嵌入可分离性、模型转移性、超参数鲁棒性等多维度消融。
- **算法实用性强**：无需完整 loss 计算，只需少量前向传播获得嵌入；算法简单，可扩展至大规模数据。
- **与现有基线公平比较**：参考了 [ACH+24] 的聚类敏感性采样，并区分了加权 vs 均匀权重，明确了自身贡献。

## 8. 不足与局限

- **实验覆盖局限**：
  - 仅评估了文本 / 结构化数据，未涉及图像 / 多模态场景，无法判断视觉任务上的泛化性。
  - 模型规模仅到 8B，未测试更大模型（如 70B/400B）的微调场景，可能与 LoRA 等参数高效微调方法的交集未深入探讨。
- **理论假设强**：Assumption 2.1 假设损失在子空间外呈二次增长，且子空间内可分解。许多损失（如对抗性损失、非平滑损失）不一定满足该条件，限制了理论适用范围。
- **超参数敏感性**：$\lambda$ 和 $\gamma$ 依赖调优，文中虽给出热力图分析，但未提供自适应选择机制，实际使用中需要额外验证集或交叉验证。
- **计算资源未详细报告**：缺少 GPU 卡时数、内存占用等详细信息，影响可复现性。
- **缺乏可复现代码 / 公开仓库**：文中未提供代码链接或伪代码的精确实现细节，不利于同行复现或扩展。
- **统计显著性测试缺失**：文中仅报告均值和标准差，未使用 $t$ 检验或置信区间验证方法间差异的显著性，部分差距可能不显著（如 Table 1 中低秩 vs 聚类 SS 的差异）。
- **未讨论负采样或冗余控制**：算法采用独立采样，可能导致子集内冗余，影响下游训练效率；未与子模优化类的多样性约束方法（如 Graph Cut）进行深入结合。

（完）
