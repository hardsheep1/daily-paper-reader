---
title: "Clusters are All You Need: Pre-Training the Tsetlin Machine with Semantic Clusters from Language Models for Interpretability"
title_zh: 聚类即一切：利用来自语言模型的语义聚类预训练Tsetlin机以实现可解释性
authors: "Jiechao Gao, Rohan Kumar Yadav, Yuangang Li, Yuandong Pan, Jie Wang, Ying Liu, Michael Lepech"
date: 2026-06-18
pdf: "https://arxiv.org/pdf/2606.19815v1"
tags: ["query:ac"]
score: 8.0
evidence: 利用语言模型嵌入对文本聚类以预训练Tsetlin机器
tldr: 预训练语言模型如BERT在文本分类中表现优异但缺乏可解释性，而Tsetlin Machine (TM)虽可解释但语义捕获不足。本文提出一种语义预训练框架，利用K-means或Top2Vec将文本聚类为语义簇，用簇-样本对预训练非否定TM，使其学习可解释的语义关键词再微调。在五个数据集上，该方法显著优于朴素和基于嵌入的TM，性能接近BERT且保持可解释性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.19815v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 750, \"height\": 321, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.19815v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1290, \"height\": 471, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.19815v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 645, \"height\": 482, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.19815v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 694, \"height\": 534, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.19815v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1636, \"height\": 618, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.19815v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1651, \"height\": 422, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.19815v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 521, \"height\": 277, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.19815v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 832, \"height\": 356, \"label\": \"Table\"}]"
motivation: 预训练语言模型可解释性差，而Tsetlin Machine虽可解释但语义信息不足，需要一种无需嵌入的语义知识迁移方法。
method: 通过聚类（K-means/Top2Vec）生成语义簇，用簇-样本对预训练非否定TM（增强Type I反馈），再微调以适应下游任务。
result: 在五个数据集上，该方法大幅超越基线TM，性能媲美BERT，同时保持完全可解释性。
conclusion: 语义预训练框架有效将语言模型知识迁移至TM，在可解释性与性能间取得平衡。
---

## 摘要
诸如BERT之类的预训练语言模型在文本分类任务中表现出色，但缺乏透明度，限制了其在高风险场景中的应用。Tsetlin机提供了完全可解释的基于子句的推理，但捕获的语义信息很少，之前将两者结合的尝试依赖于静态词嵌入，无法捕捉上下文意义。我们提出了一种语义预训练框架，在不使用嵌入的情况下将知识从预训练语言模型迁移到Tsetlin机中。使用K-means或Top2Vec将文本样本分组为语义连贯的聚类，生成的聚类-样本对通过增强的I型反馈预训练非否定Tsetlin机。因此，Tsetlin机学习到可解释的语义关键词，并在下游任务上进行微调。在五个数据集上，我们的方法显著优于普通和基于嵌入的Tsetlin机，性能与BERT相当，同时保持可解释性。

## Abstract
Pre-trained language models such as BERT achieve strong text classification performance but lack transparency, limiting their use in high-stakes settings. The Tsetlin Machine (TM) offers fully interpretable, clause-based reasoning but captures little semantic information, and prior attempts to bridge the two rely on static word embeddings that miss contextual meaning. We propose a semantic pre-training framework that transfers knowledge from a pre-trained language model into a TM without using embeddings. Text samples are grouped into semantically coherent clusters with K-means or Top2Vec, and the resulting cluster-sample pairs pre-train a non-negated TM with enhanced Type I feedback. The TM thereby learns interpretable semantic keywords that are fine-tuned on downstream tasks. Across five datasets, our method substantially outperforms vanilla and embedding-based TMs and reaches performance competitive with BERT while remaining interpretable.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：预训练语言模型（如BERT）在文本分类任务中表现优异，但其黑箱性质限制了在高风险领域（如法律、医疗）中的应用。Tsetlin Machine（TM）以其基于子句的逻辑推理提供了完全可解释性，但仅依赖词袋表示，缺乏语义信息，导致性能远低于神经模型。
- **先前尝试的不足**：已有工作将静态词嵌入（如GloVe）与TM结合，但静态嵌入无法捕捉上下文语义，且尚未有可解释方法将基于Transformer的模型（如BERT）集成到TM中。
- **本文目标**：提出一个无需嵌入的语义预训练框架，将预训练语言模型的语义知识迁移到TM中，在保持TM可解释性的同时提升其分类性能，使其接近BERT。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **整体框架**：两阶段流程（见图1）。
  1. **预训练阶段**：使用未标注数据，通过预训练语言模型（BERT变体）或Top2Vec生成文档嵌入并聚类（K-means或层次密度聚类），得到语义簇。每个文档被赋予一个簇标签，形成簇-样本对。然后用这些对预训练**非否定Tsetlin Machine（NTM）**，NTM是TM的变体，关键修改包括：
     - **增强的Type I反馈**：将正反馈概率固定为1.0，惩罚概率置为0，优先强化正确分类的字面量。
     - **禁用否定文字**：每个子句只包含原始输入特征（非否定形式），降低复杂度，提升可解释性。
  2. **微调阶段**：对带标签的文本，首先用相同聚类方法得到其簇标签，然后提取该簇对应的NTM高置信度关键词（基于TA状态总和），将这些关键词作为额外特征扩充原文本的词袋表示，最后用标准TM（重新启用否定文字）进行微调。
- **置信度定义**：文字j在类别y下的置信度 Conf_y(j) = Σ_{i∈Clauses_positive(y)且包含j} TA_state_{ij}，TA状态越深表示越重要。
- **关键参数**：NTM使用2000个子句（每簇1000），阈值T=4000，特异性s=10；微调时s降为5以增加灵活性。

## 3. 实验设计：数据集、基准、对比方法

- **数据集**：五个英文主题分类数据集：
  - AG-News（4类，30000训练/7600测试）
  - NYT-Topics（9类，30000/3000）
  - DBpedia（14类，30000/70000）
  - R8（Reuters子集，8类，5485/2189）
  - R52（Reuters子集，52类，6532/2568）
- **对比方法（9个基线）**：
  - 传统方法：BoW TFIDF、char-CNN、LSTM（无/有GloVe）、fastText
  - 预训练模型：BERT-base、BERT-large
  - TM变体：vanilla TM、TM with GloVe、Top2Vec（仅用于关键词对比）
- **实验设置**：预训练使用AG-News的未标注数据，聚类数选择{50,100,200,300,400,500}进行调优。所有TM结果报告均值±标准差。

## 4. 资源与算力

- 论文**未明确说明**使用的GPU型号、数量或训练时长。仅在实验细节中给出超参数，但未提及计算资源。因此无法评估其算力成本。

## 5. 实验数量与充分性

- **主要实验**：表1展示了在5个数据集上与9个基线的性能对比，每个方法均报告准确率。
- **消融与调优实验**：
  - 表2：不同聚类大小（50-500）对性能的影响，在3个数据集上验证。
  - 图3：不同数量高置信度关键词（5~50）对准确率的影响，在5个数据集上展示。
  - 消融：对比NTM预训练使用Top2Vec vs BERT-base vs BERT-large的差异。
- **充分性与客观性**：实验比较全面，涵盖了传统、神经、可解释模型等多个类别；使用了公开标准数据集，结果有重复标准差；聚类大小调优参数扫描合理。但缺乏在更多领域（如医疗、法律）或非英文数据上的验证，且未与决策树、逻辑回归等其他可解释模型直接对比。

## 6. 论文的主要结论与发现

- **核心结论**：预训练TM with BERT-derived clusters显著提升TM性能，在5个数据集上准确率接近BERT-large（差距多在1-2%内），同时保持完全可解释性。
- **聚类大小影响**：聚类数200为最优，过大或过小均导致性能下降。
- **高置信度关键词数量**：约20个关键词时达到最佳性能，过多会引入噪声。
- **与Top2Vec对比**：基于BERT的NTM预训练优于Top2Vec方式（高出1-2%）。
- **可解释性验证**：通过示例（体育簇）展示了NTM学习到的单调子句模式，以及微调后加入关键词减少对否定文字的依赖，提升语义理解。

## 7. 优点：方法或实验设计上的亮点

- **创新性**：首次提出利用预训练语言模型（BERT）的聚类语义预训练TM，无需嵌入层，直接迁移上下文知识。
- **可解释性保持**：NTM禁用否定文字，子句为纯合取，易于人类理解；微调后虽启用否定，但通过注入关键词仍可保持较简单的子句模式。
- **实用性**：聚类和预训练只需执行一次，可复用至多个下游任务，摊销离线成本。
- **实验设计全面**：对比了多种基线（传统、神经、可解释），并系统研究了聚类大小和关键词数量的影响。

## 8. 不足与局限

- **实验覆盖不足**：仅在英文主题分类数据集上验证，未涉及情感分析、法律/医学文本等高风险领域；未在低资源或噪声数据上测试。
- **性能差距**：仍略逊于BERT-large（约0.3%~2%），且对聚类质量敏感，可能依赖预训练语言模型的嵌入质量。
- **计算成本**：尽管预训练可复用，但聚类和NTM训练仍有离线开销，且未报告具体算力需求。
- **对比范围有限**：未与其他可解释模型（如决策树、稀疏逻辑回归、可解释规则学习器）进行直接比较。
- **潜在偏差**：预训练聚类仅在AG-News上进行，虽声称泛化性好，但未在目标数据集上验证聚类迁移的稳健性。

（完）
